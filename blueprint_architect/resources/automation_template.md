# 🛠️ 最強の自動化設計図：[自動化ツール名]

> **作成日**: [YYYY-MM-DD]
> **概要**: [コンセプトを一言で]
> **トリガー機能**: [どうやって起動するか。例: 特定の文字を打つ、時間が来る等]

---

## 1. 自動化要件 (MVPフォーカス)

### 必須要件（Must: Day 1で確実に動かす範囲）
- [ ] 

### 推奨要件（Want - 将来実装）
- [ ] 

---

## 2. 既存エコシステムの再利用（車輪の再発明防止）
- **利用するローカルの既存ツール/スキル**: [Ritsuto Brain内にある流用可能なスキル、APIスクリプトなどをリストアップ。無い場合は「なし」]

---

## 3. プロンプトチェーンとファイル構成

### [Phase 1: 情報収集・トリガー]
- 対象ファイル: `入力データ` または `特定のフォルダ`
- **【入力データ形式】**: [どんなデータ形式で入ってくるか（例: CSVのテキスト、画像バイナリ、単なるMarkdown文字等）]
- 処理内容: 
- **【出力データ形式】**: [次のフェーズへ渡すデータ形式（例: JSON文字列、要約されたテキスト塊等）]

### [Phase 2: プロンプト実行]
- 対象ファイル: `01_step1_prompt.md` 
- **【入力データ形式】**: [Phase 1の出力形式]
- 処理内容: [この段階でAIに何をさせるか]
- **【出力データ形式】**: [次のフェーズへ渡すデータ形式]

### [Phase 3: 評価・出力]
- 対象ファイル: `02_step2_eval.md` または `出力ファイル.md`
- **【入力データ形式】**: [Phase 2の出力形式]
- 処理内容: [結果の出力先など]
- **【最終出力形式】**: [例: ファイルとして保存、SlackへAPI投稿等]

---

## 4. セキュリティとデータ保護
- [ ] APIキーが必要な場合は `.env` を使用し、絶対に直書きしない。
- [ ] プロンプトインジェクション対策（ユーザー入力をシステムプロンプトより上位で評価しない等）を考慮する。

---

## 5. 完成の定義（QA・受け入れテスト）
> ※「動いた」ではなく、「要件を満たしているか」を判定する最終テストの条件です。
- **💥 正常系（Happy Path）テスト**:
  - テスト手順: [例：サンプルの〇〇テキストを入力として与える]
  - 期待される結果: [例：エラーなく処理が完走し、期待したフォーマットのJSONが出力フォルダに保存されること]
- **💥 異常系（Edge Case）テスト**:
  - テスト手順: [例：空のファイル、または指定外の形式（画像など）を入力として与える]
  - 期待される結果: [例：AIが処理を停止し、「対応していないファイル形式です」というエラーメッセージを出力して終了すること]

---

## 6. 実装AIへのファースト・プロンプト（初手指示）

> [!TIP]
> 以下のプロンプトをコピーして、実装担当のAI（Antigravity等）に送信してください。一度に全体を作らせず、安全に一番最初のプロンプトやスクリプトから開発をスタートさせます。

```text
あなたは天才プログラマー・自動化エンジニアです。
提供された設計図「[このMarkdownのファイル名]」を読み込み、内容を完璧に理解してください。
その後、設計図の【Phase 1】に必要なファイル（フォルダ構成と最初のプロンプトなどの枠組み）のみを作成してください。
※絶対に次のステップの処理には進まないでください。Phase 1のファイルを出力し、私が中身を確認して「受け入れテスト」をクリアするまで待機してください。
```
