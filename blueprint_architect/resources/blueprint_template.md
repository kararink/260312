# 🛠️ 最強の設計図：[アプリ名]

> **作成日**: [YYYY-MM-DD]
> **概要**: [コンセプトを一言で]
> **ターゲット**: [誰向けか。規模感（個人/チーム/一般）]
> **動作環境**: [ブラウザ/拡張機能/デスクトップ/アプリ等]

---

## 1. 機能要件 (MVPフォーカス)

### 必須要件（Must）
- [ ] 
- [ ] 

### 推奨要件（Want - 将来実装）
- [ ] 
- [ ] 

### 非機能要件（制約・スケーラビリティ）
- **想定トラフィック**: [想定データ量・頻度]
- **許容速度**: [レスポンスの妥協可能ライン等]

---

## 2. アーキテクチャと技術スタック

- **Frontend**: [HTML5, CSS3, Vanilla JavaScript 等（レスポンシブWebデザイン必須）]
- **Backend/API**: [使用するAPIやサーバー言語]
- **Database**: [Firebase推奨。Supabase禁止。不要ならGAS等]
- **Authentication**: [Google認証推奨]
- **Storage**: [LocalStorage 等]
- **Second Brain Ecosystem**: [Ritsuto Brain内の既存スキル(visual_designer等)や既存APIの再利用方針があれば記載]

---

## 3. データ構造と処理フロー

### データベース設計（あるいはデータ保存場所）
| コレクション/保存先 | フィールド名 | データ型 | 説明 |
| :--- | :--- | :--- | :--- |
| 例: `users` | `uid` | string | Google認証のUID |
| 例: `posts` | `content` | string | 投稿内容 |

### 主要データフロー
1. ユーザーが [アクション] を行う
2. システムが [判定・処理] を実行
3. データが [保存先] に [形式] で保存される

---

## 4. ファイル構成と役割

```text
📁 root/
 ├── index.html        # メインUI構成
 ├── styles.css        # スタイル定義
 ├── main.js           # コアロジック
 └── config.js         # 設定・Firebase等初期化
```

---

## 5. セキュリティ・リーガル宣誓

> [!CAUTION]
> 開発エージェントは以下のルールを **絶対遵守** して実装を進めてください。

- **機密情報の保護**: APIキーや環境変数は絶対にハードコードせず、`.env` 等外部から読み込むこと。
- **XSS/CSRF対策**: 入力値のサニタイズ、不正なスクリプト実行を防ぐ実装を徹底すること。
- **データ露出防止**: サーバーサイド処理の原則や、Firebase Security Rulesの適切な設定を必ず初手で行うこと。
- **利用規約・ポリシー**: [必要に応じて記載。アプリ公開時にプライバシーポリシーが必要か等]

---

## 6. 実装ステップと受け入れテスト

> ※手戻りを防ぐため、以下の順序を厳守し、各Stepごとに「受け入れテスト」をクリアしてから次に進むこと。

### Step 1: 土台作成 (UI/Mockup)
- **やること**: 必須となるHTML骨格とCSS（レスポンシブ）の作成
- **受け入れテスト**: [例：スマホサイズでもデザインが崩れていないか確認できること]

### Step 2: 認証・DB接続の基盤 (Firebase等)
- **やること**: Firebaseのプロジェクト設定、Google認証の組み込み（必要な場合）
- **受け入れテスト**: [例：ログインボタンを押して、Googleアカウントの認証画面が起動すること]

### Step 3: コアロジックの実装（Must要件）
- **やること**: メイン機能となるJavaScriptロジックの実装
- **受け入れテスト**: [例：文字を入力して「保存」を押した際、DBにデータが書き込まれること]

### Step 4: UIインタラクションとエラーハンドリング
- **やること**: ユーザーの操作に対するフィードバック、エラー画面、セキュリティ対策ルールの仕上げ
- **💥 受け入れテスト（正常系）**: [例：全ての項目を入力して「保存」を押した際、完了画面へ遷移すること]
- **💥 受け入れテスト（異常系）**: [例：必須項目を空にして「保存」を押した際、送信されずに「必須項目です」という赤文字のエラーが表示されること]

---

## 7. 実装AIへのファースト・プロンプト（初手指示）

> [!TIP]
> 以下のプロンプトをコピーして、実装担当のAI（Antigravity等）に送信してください。一度に全体を作らせず、安全にStep 1から開発をスタートさせます。

```text
あなたは天才プログラマーです。
提供された設計図「[このMarkdownのファイル名]」を読み込み、内容を完璧に理解してください。
その後、設計図の【Step 1: 土台作成 (UI/Mockup)】のみを実装してください。
※絶対にStep 2以降の処理（Firebase接続やロジックの実装など）には進まないでください。Step 1のコードを出力し、私が動作確認して「受け入れテスト」をクリアするまで待機してください。
```
